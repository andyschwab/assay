# negative/packet-secret-shaped — a credential's real value pasted in where the
# format asks only for a name; validate-packet must refuse it (fail-closed).
packet: 1
yardstick: 0
answered:
  date: "2026-09-01"
  by: founder
  via: owner-prompt
custody:
  credentials:
    - name: EMAIL_SEND_API_KEY
      used_by: notification service
      lives: "sk-ThisLooksLikeARealSecretKeyValue123456"
      readers: [founder]
      rotated: never
      leak_noticed: "no"
