node_modules/
*.log
# rendered outputs (regenerated from the Markdown source of truth)
*.pdf
*.preview.png
# a run compiled inside a checkout
eval/raw/
# local environment files — never committed (credentials live outside the repo)
.env
.env.*
